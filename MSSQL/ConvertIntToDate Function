-- Usage:
-- SELECT [dbo].[ConvertIntToDate](1613172943151);

CREATE OR ALTER FUNCTION [dbo].[ConvertIntToDate] (@bigint BIGINT)
RETURNS DATETIME
AS
BEGIN
	RETURN DATEADD(s, CONVERT(BIGINT, @bigint) / 1000, CONVERT(DATETIME, '01-01-1970 00:00:00'));
END
